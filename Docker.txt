docker run --rm --label=jekyll --volume=$(pwd):/srv/jekyll   -it -p $(docker-machine ip `docker-machine active`):4000:4000  jekyll/jekyll:pages jekyll serve --force_polling .


docker run -it --name nanoc --volume=$(pwd):/srv/nanoc -p $(docker-machine ip `docker-machine active`):3000:3000 ruby:latest /bin/bash