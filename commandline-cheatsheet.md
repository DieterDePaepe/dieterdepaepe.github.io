# Anaconda (Python)

Create a new environment: `conda create -n myEnvName python=3.5 anaconda` (installs all default anaconda packages)

Activate an environment: `activate myEnvName` (prepend `source ` on linux)
